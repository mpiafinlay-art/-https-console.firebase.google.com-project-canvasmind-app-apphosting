'use client';

import { useEffect } from 'react';

export default function FirebaseErrorListener() {
  useEffect(() => {
    // Listener para errores de Firebase (placeholder)
    return () => {
      // Cleanup
    };
  }, []);

  return null;
}

